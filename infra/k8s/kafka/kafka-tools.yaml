apiVersion: v1
kind: Pod
metadata:
  name: kafka-tools
  namespace: kafka
spec:
  restartPolicy: Always
  containers:
    - name: kafka-tools
      image: quay.io/strimzi/kafka:0.41.0-kafka-3.6.0
      command: ["sleep", "infinity"]