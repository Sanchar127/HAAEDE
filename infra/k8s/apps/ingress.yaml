apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recovery-ingress
  namespace: recovery
spec:
  rules:
    - host: recovery.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-service
                port:
                  number: 80
